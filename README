ANF-to-CNF converter. It takes as input a file containing an ANF like:

------------
x1*x2 + 1
x1 + x3 + x4
------------

and generates an optimalised CNF from it. It can also solve the ANF
using CryptoMiniSat. Please read the help carefully:

$ ./anfconv -h 
